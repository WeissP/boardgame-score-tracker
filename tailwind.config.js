const colors = require('tailwindcss/colors')

module.exports = {
  mode: '',
  theme: {
    extend: {
      colors: {
        sky: colors.sky,
        cyan: colors.cyan,
      },
    },
  },
  variants: {},
  plugins: [],
}
